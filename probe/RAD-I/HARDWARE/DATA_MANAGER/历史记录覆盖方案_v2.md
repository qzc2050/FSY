# 5 分钟历史记录覆盖方案

## 一、Flash 存储架构

### 1.1 扇区布局

```
外部 QSPI Flash（W25Q64JV：8MB）布局：

地址：0x00000000
┌─────────────────────────┐
│ 元数据扇区              │ 4KB（1 个扇区）
├─────────────────────────┤
│ 正式扇区 0              │ 4KB（主数据区）
│ 正式扇区 1              │ 4KB（主数据区）
│ ...                     │
│ 正式扇区 N-1            │ 4KB（主数据区）
├─────────────────────────┤
│ 缓存扇区 0              │ 4KB（与正式扇区一一对应）
│ 缓存扇区 1              │ 4KB
│ ...                     │
│ 缓存扇区 N-1            │ 4KB
├─────────────────────────┤
│ 临时扇区                │ 4KB（独立扇区，时间调整时用）
├─────────────────────────┤
│ 配置区（64KB）          │ Flash 末尾
└─────────────────────────┘

参数定义：
- DATA_5_MIN_MAX_RECORDS = 300（最大记录数，示例值）
- EXT_FLASH_DM_SLOTS_PER_SECTOR = 512（每扇区记录数）
- EXT_FLASH_DM_DATA_SECTOR_COUNT = 4（正式扇区数量，示例值）
- EXT_FLASH_DM_CACHE_SECTOR_COUNT = 4（缓存扇区数量，示例值）
- 临时扇区数量 = 1（独立扇区）

注意：
- 正式扇区数量和缓存扇区数量可以根据实际需求配置
- 本文档示例中使用 4 个正式扇区 +4 个缓存扇区
```

### 1.2 扇区类型说明

| 扇区类型 | 用途 | 数量 | 特性 |
|---------|------|------|------|
| **正式扇区** | 稳定数据存储 | N 个 | 环形存储，满后不主动擦除 |
| **缓存扇区** | 活动数据写入 | N 个 | 与正式扇区一一对应，新记录优先写入 |
| **临时扇区** | 时间调整缓冲 | 1 个 | 独立扇区，时间调整时临时使用 |

### 1.3 正常写入流程

```
原则：
1. 新记录始终写入缓存扇区
2. 缓存扇区满后，迁移到**对应编号**的正式扇区
3. 正式扇区满后，循环覆盖最旧扇区

流程：
第 1 条 → 缓存扇区 0 的第 0 条
第 2 条 → 缓存扇区 0 的第 1 条
...
第 512 条 → 缓存扇区 0 满，触发迁移
  → 擦除正式扇区 0
  → 复制缓存扇区 0 到正式扇区 0
  → 擦除缓存扇区 0
  → 新记录写入缓存扇区 1 的第 0 条
...
第 N 条（缓存扇区 N-1 写满）：
  → 擦除正式扇区 N-1（**对应编号**的正式扇区）
  → 复制缓存扇区 N-1 到正式扇区 N-1
  → 擦除缓存扇区 N-1
  → 新记录写入**下一个缓存扇区**的第 0 条（循环）

示例（4 个扇区循环）：
- 缓存扇区 0 满 → 迁移到正式扇区 0 → 写入缓存扇区 1
- 缓存扇区 1 满 → 迁移到正式扇区 1 → 写入缓存扇区 2
- 缓存扇区 2 满 → 迁移到正式扇区 2 → 写入缓存扇区 3
- 缓存扇区 3 满 → 迁移到正式扇区 3 → 写入缓存扇区 0（循环）
```

---

## 二、时间调整覆盖方案

### 2.1 核心原则

```
1. 新记录始终优先写入缓存扇区
2. 时间调整时，只操作缓存扇区和临时扇区
3. 正式扇区的数据保持稳定，不轻易擦除
4. 读取时自动判断正式扇区 + 缓存扇区（或临时扇区）
5. 时间回调时，保留有效记录，覆盖无效记录
6. 时间调快时，恢复之前被隐藏的记录
```

### 2.2 时间回调流程（需要覆盖）

#### 场景 A：未满一轮，需要覆盖当前缓存扇区（使用独立扇区）

```
重要概念：
- 未满一轮 = 还有空白正式扇区
- 已写入的缓存扇区满后，数据迁移到正式扇区，缓存扇区被擦除
- 当前只有一个缓存扇区正在使用（活动写入）
- 有效记录数 = 已写入正式扇区的有效数 + 当前缓存扇区的有效数
- 时间回调需要覆盖**当前缓存扇区**时，使用独立扇区作为临时缓冲
- 假设：已写入的正式扇区时间戳较早，全部≤新系统时间，所以全部有效

初始状态：
- 正式扇区 0-1：已写满（2×512=1024 条，缓存扇区 0-1 已擦除）
- 正式扇区 2-3：空白（未使用）
- 缓存扇区 0-1：已擦除（数据已迁移到正式扇区）
- 缓存扇区 2：正在写入（第 80 条）
- 缓存扇区 3：空白
- 独立扇区：空白
- 总有效记录数：1024 + 80 = 1104 条
- 系统时间：18:00（时间戳 20000）

操作：时间回调到 12:00（时间戳 15000）
需求：缓存扇区 2 的前 19 条有效，从第 20 条开始覆盖

步骤 1：扫描所有扇区
- 正式扇区 0-1：全部有效（1024 条，时间戳≤15000）
- 缓存扇区 2：前 19 条有效（时间戳≤15000），后 61 条无效（时间戳>15000）

步骤 2：处理独立扇区（关键！）
- 擦除独立扇区
- 读取缓存扇区 2 的前 19 条到 RAM
- 写入独立扇区的前 19 条
- 记录：独立扇区缓存的是"缓存扇区 2"的数据

步骤 3：更新元数据
- current_system_ts = 15000
- valid_count = 1024 + 19 = 1043 条
- formal_valid_count = 1024
- current_sector_type = TEMP（使用独立扇区）
- temp_write_next = 19
- temp_sector_cached_source = 2（缓存的是缓存扇区 2）
- 保存元数据

步骤 4：写入新记录
- 新记录写入独立扇区的第 20 条
- valid_count++

结果：
- 有效记录：1043 条 + 新写入的记录
- 当前写入：独立扇区的第 20 条
- 缓存扇区 2：保持原样（19 条有效 + 61 条无效，但不使用）
- 独立扇区：19 条有效记录 + 新记录

后续处理（独立扇区写满后）：
当独立扇区写满（第 512 条）：
1. 擦除正式扇区 2
2. 将独立扇区的 512 条复制到正式扇区 2
3. 擦除独立扇区
4. 擦除缓存扇区 2
5. 新记录写入缓存扇区 3 的第 0 条
6. 更新元数据
```

#### 场景 B：未满一轮，需要覆盖已完成的正式扇区（不使用独立扇区）

```
重要概念：
- 时间回调需要覆盖的是**已完成的正式扇区**（而非当前缓存扇区）
- 此时直接将正式扇区的有效记录复制到对应的缓存扇区
- **不需要使用独立扇区**
- 关键：需要扫描所有扇区，确定哪些记录恢复有效

初始状态：
- 正式扇区 0-1：已写满（2×512=1024 条）
- 正式扇区 2-3：空白（未使用）
- 缓存扇区 0-1：已擦除（数据已迁移到正式扇区）
- 缓存扇区 2：正在写入（第 100 条）← 回调前的写入位置
- 缓存扇区 3：空白
- 独立扇区：空白
- 总有效记录数：1024 + 100 = 1124 条
- 系统时间：18:00（时间戳 20000）
- 读取范围：从 **正式扇区 0 的第 0 条** 开始，到 **缓存扇区 2 的第 100 条**
- 环形顺序：正式扇区 0 → 正式扇区 1 → 缓存扇区 2

操作：时间回调到 12:00（时间戳 15000）
需求：需要从**正式扇区 1**的第 32 条开始覆盖（前 31 条有效）

步骤 1：扫描所有扇区
- 正式扇区 0：全部有效（512 条，时间戳≤15000）
- 正式扇区 1：前 31 条有效（时间戳≤15000），后 481 条无效
- 缓存扇区 2：100 条无效（时间戳>15000）

步骤 2：复制到缓存扇区（关键！）
- 擦除缓存扇区 1
- 读取正式扇区 1 的前 31 条到 RAM
- 写入缓存扇区 1 的前 31 条
- 更新：cache_write_next = 31

步骤 3：更新元数据
- current_system_ts = 15000
- valid_count = 512（正式扇区 0）+ 31（缓存扇区 1）= 543 条
  （注意：缓存扇区 2 的 100 条无效，因为时间戳>15000）
- formal_valid_count = 512（只有正式扇区 0）
- current_sector_type = CACHE
- current_sector_idx = 1（切换到缓存扇区 1）
- 保存元数据

步骤 4：写入新记录
- 新记录写入缓存扇区 1 的第 32 条
- valid_count++

结果：
- 有效记录：543 条 + 新写入的记录
- 当前写入：缓存扇区 1 的第 32 条
- 读取范围：从 正式扇区 0 的第 0 条 开始，到缓存扇区 1 的最后一条
- 正式扇区 1：保持原样（逻辑上无效，但物理保留）
- 缓存扇区 2：100 条记录（逻辑上无效，但物理保留，**时间调快时恢复有效**）
- 独立扇区：未使用（空白）
- **注意：如果后续时间调快，缓存扇区 1 的 31 条数据将变为无效**（被跳过）

后续处理：
- 缓存扇区 1 写满后（第 512 条）：
  1. 擦除正式扇区 1
  2. 将缓存扇区 1 复制到正式扇区 1
  3. 擦除缓存扇区 1
  4. **使用临时扇区**（关键！）
     - 擦除临时扇区
     - 新记录写入临时扇区的第 0 条
     - 更新元数据：current_sector_type = TEMP
     - **缓存扇区 2 保持原样（100 条无效数据，但不擦除，时间调快时恢复）**
  5. 临时扇区写满后：
     - 擦除正式扇区 2
     - 将临时扇区复制到正式扇区 2
     - 擦除临时扇区
     - 擦除缓存扇区 2
     - 新记录写入缓存扇区 3 的第 0 条
```

#### 场景 C：已满一轮，需要覆盖当前缓存扇区（使用独立扇区）

```
重要概念：
- 已满一轮 = 所有正式扇区都已写入
- 时间回调需要覆盖**当前缓存扇区**时，使用独立扇区作为临时缓冲
- 与场景 A 类似，只是正式扇区已全部使用
- 关键：正式扇区中部分记录恢复有效，只有伪覆盖的部分无效

初始状态：
- 正式扇区 0-3：已写满（4×512=2048 条）
- 缓存扇区 0-2：已擦除（数据已迁移到正式扇区）
- 缓存扇区 3：正在写入（第 500 条）← 回调前的写入位置
- 独立扇区：空白
- 总有效记录数：2048 + 500 = 2548 条
- 系统时间：18:00（时间戳 20000）
- 读取范围：从 正式扇区 3 的第 501 条 开始，到缓存扇区 3 的第 500 条
- 正式扇区 3 的前 500 条 是"伪覆盖"（逻辑上无效，物理上保留）

操作：时间回调到 12:00（时间戳 15000）
需求：缓存扇区 3 的前 99 条有效，从第 100 条开始覆盖

步骤 1：扫描所有扇区
- 正式扇区 3：第 100-512 条有效（413 条，时间戳≤15000）
- 正式扇区 0-2：全部有效（1536 条，时间戳≤15000）
- 缓存扇区 3：前 99 条有效（时间戳≤15000），后 401 条无效

步骤 2：处理独立扇区（关键！）
- 擦除独立扇区
- 读取缓存扇区 3 的前 99 条到 RAM
- 写入独立扇区的前 99 条
- 记录：独立扇区缓存的是"缓存扇区 3"的数据

步骤 3：更新元数据
- current_system_ts = 15000
- valid_count = 413（正式扇区 3）+ 1536（正式扇区 0-2）+ 99（独立扇区）= 2048 条
- formal_valid_count = 1949（正式扇区 3 的 413 条 + 正式扇区 0-2 的 1536 条）
- current_sector_type = TEMP（使用独立扇区）
- temp_write_next = 99
- temp_sector_cached_source = 3（缓存的是缓存扇区 3）
- 保存元数据

步骤 4：写入新记录
- 新记录写入独立扇区的第 100 条
- valid_count++

结果：
- 有效记录：2048 条 + 新写入的记录
- 当前写入：独立扇区的第 100 条
- 读取范围：从 正式扇区 3 的第 100 条 开始 → 正式扇区 0 → 正式扇区 1 → 正式扇区 2 → 独立扇区
- 缓存扇区 3：保持原样（99 条有效 + 401 条无效，但不使用）
- 正式扇区 3：前 99 条逻辑上无效（已复制到独立扇区），第 100-512 条有效
- 独立扇区：99 条有效记录 + 新记录

后续处理（独立扇区写满后）：
当独立扇区写满（第 512 条）：
1. 擦除正式扇区 3（最旧的扇区）
2. 将独立扇区的 512 条复制到正式扇区 3
3. 擦除独立扇区
4. 擦除缓存扇区 3
5. 新记录写入缓存扇区 0 的第 0 条（循环）
6. 更新元数据
```

#### 场景 D：已满一轮，需要覆盖已完成的正式扇区（不使用独立扇区）

```
重要概念：
- 时间回调需要覆盖的是**已完成的正式扇区**（而非当前缓存扇区）
- 此时直接将正式扇区的有效记录复制到对应的缓存扇区
- **不需要使用独立扇区**
- 关键：其他正式扇区的记录恢复有效，只有被覆盖的正式扇区部分无效

初始状态：
- 正式扇区 0-3：已写满（4×512=2048 条）
- 缓存扇区 0-2：已擦除（数据已迁移到正式扇区）
- 缓存扇区 3：正在写入（第 100 条）← 回调前的写入位置
- 独立扇区：空白
- 总有效记录数：2048 + 100 = 2148 条
- 系统时间：18:00（时间戳 20000）
- 读取范围：从 正式扇区 3 的第 101 条 开始，到缓存扇区 3 的第 100 条
- 正式扇区 3 的前 100 条 是"伪覆盖"（逻辑上无效，物理上保留）

操作：时间回调到 12:00（时间戳 15000）
需求：需要从**正式扇区 2**的第 50 条开始覆盖（前 49 条有效）

步骤 1：扫描所有扇区
- 正式扇区 2：第 50-512 条有效（463 条，时间戳≤15000）
- 正式扇区 3：全部有效（512 条，时间戳≤15000）
- 正式扇区 0-1：全部有效（1024 条，时间戳≤15000）
- 缓存扇区 3：100 条无效（时间戳>15000）

步骤 2：复制到缓存扇区（关键！）
- 擦除缓存扇区 2
- 读取正式扇区 2 的前 49 条到 RAM
- 写入缓存扇区 2 的前 49 条
- 更新：cache_write_next = 49

步骤 3：更新元数据
- current_system_ts = 15000
- valid_count = 463（正式扇区 2）+ 512（正式扇区 3）+ 1024（正式扇区 0-1）+ 49（缓存扇区 2）= 2048 条
- formal_valid_count = 1999（正式扇区 2-3 的 975 条 + 正式扇区 0-1 的 1024 条）
- current_sector_type = CACHE
- current_sector_idx = 2（切换到缓存扇区 2）
- 保存元数据

步骤 4：写入新记录
- 新记录写入缓存扇区 2 的第 50 条
- valid_count++

结果：
- 有效记录：2048 条 + 新写入的记录
- 当前写入：缓存扇区 2 的第 50 条
- 读取范围：从 正式扇区 2 的第 50 条 开始 → 正式扇区 3 → 正式扇区 0 → 正式扇区 1 → 缓存扇区 2
- 正式扇区 2：前 49 条逻辑上无效（已复制到缓存扇区 2），第 50-512 条有效
- 正式扇区 3：全部有效（恢复有效）
- 缓存扇区 3：100 条记录无效（时间戳>15000，不使用）
- 独立扇区：未使用（空白）

后续处理：
- 缓存扇区 2 写满后（第 512 条）：
  1. 擦除正式扇区 2
  2. 将缓存扇区 2 复制到正式扇区 2
  3. 擦除缓存扇区 2
  4. **使用独立扇区**（关键！）
     - 擦除独立扇区
     - 新记录写入独立扇区的第 0 条
     - 更新元数据：current_sector_type = TEMP
     - **缓存扇区 3 保持原样（100 条无效数据，但不擦除，时间调快时恢复）**
  5. 独立扇区写满后：
     - 擦除正式扇区 3
     - 将独立扇区复制到正式扇区 3
     - 擦除独立扇区
     - 擦除缓存扇区 3
     - 新记录写入缓存扇区 0 的第 0 条（循环）
```

### 2.3 时间调快流程（恢复记录）

#### 核心原则

```
1. 时间调快时，独立扇区的数据**直接丢弃**
2. 恢复使用原来的缓存扇区（如果缓存扇区的数据时间戳≤新系统时间）
3. 扫描所有扇区，计算新的有效记录数
4. 更新元数据，继续在原缓存扇区写入

关键：
- 独立扇区是临时缓冲，时间调快时不再需要
- 原来被"隐藏"的缓存扇区记录可能恢复有效
- 有效记录数 = 正式扇区有效数 + 缓存扇区有效数

时间调快的两种情况：
1. 调快时间 > 初始时间：所有原记录恢复有效（当前示例展示的情况）
2. 初始时间 > 调快时间 > 调回时间：部分原记录恢复有效
   - 时间戳 ≤ 调快时间 的记录有效
   - 时间戳 > 调快时间 的记录仍然无效（即使它们是在时间回调前写入的）
   - 示例：初始 260605 1630 → 调回 260204 1103 → 调快 260324 1833
```

#### 场景 A：未满一轮，从独立扇区恢复（对应场景 A 的回调）

```
初始状态（时间回调后）：
- 正式扇区 0-1：已写满（1024 条）
- 正式扇区 2-3：空白
- 缓存扇区 0-1：已擦除
- 缓存扇区 2：19 条有效 + 61 条无效（但不使用）
- 独立扇区：19 条有效 + 新写入的记录（假设共 50 条）
- current_sector_type = TEMP
- temp_sector_cached_source = 2
- valid_count = 1024 + 50 = 1074 条

操作：时间调快到 20:00（时间戳 25000）

步骤 1：扫描所有扇区
- 正式扇区 0-1：全部有效（1024 条）
- 缓存扇区 2：全部有效（80 条，时间戳≤25000）
- 独立扇区：50 条（但不再需要）

步骤 2：丢弃独立扇区
- 擦除独立扇区
- current_sector_type = CACHE
- current_sector_idx = 2（恢复使用缓存扇区 2）

步骤 3：更新元数据
- current_system_ts = 25000
- valid_count = 1024 + 80 = 1104 条（恢复使用缓存扇区 2 的全部记录）
- formal_valid_count = 1024
- 保存元数据

步骤 4：继续写入
- 新记录写入缓存扇区 2 的第 81 条

结果：
- 有效记录：1104 条
- 当前写入：缓存扇区 2 的第 81 条
- 独立扇区：已擦除
```

#### 场景 B：未满一轮，恢复已隐藏的缓存扇区（对应场景 B 的回调）

```
初始状态（时间回调后）：
- 正式扇区 0-1：已写满（1024 条）
- 正式扇区 2-3：空白
- 缓存扇区 0-1：已擦除
- 缓存扇区 1：31 条有效（正在写入）
- 缓存扇区 2：100 条（逻辑上无效，但物理保留）
- current_sector_type = CACHE
- current_sector_idx = 1
- valid_count = 512 + 31 = 543 条

操作：时间调快到 20:00（时间戳 25000）

步骤 1：扫描所有扇区
- 正式扇区 0-1：全部有效（1024 条）
- 缓存扇区 1：31 条**无效**（时间调快后被跳过）
- 缓存扇区 2：100 条有效（时间戳≤25000，恢复有效！）

步骤 2：恢复缓存扇区 2
- 无需迁移数据（数据已在原位）
- **缓存扇区 1 的 31 条数据变为无效**（因为切换回缓存扇区 2，缓存扇区 1 的数据被跳过了）
- current_sector_type = CACHE
- current_sector_idx = 2（切换回缓存扇区 2）

步骤 3：更新元数据
- current_system_ts = 25000
- valid_count = 1024 + 100 = 1124 条（恢复了缓存扇区 2 的 100 条，缓存扇区 1 的 31 条无效）
- formal_valid_count = 1024
- 保存元数据

步骤 4：继续写入
- 新记录写入缓存扇区 2 的第 101 条

结果：
- 有效记录：1124 条（恢复了缓存扇区 2 的 100 条）
- 当前写入：缓存扇区 2 的第 101 条
- 缓存扇区 1：31 条数据无效（时间调快后被跳过）
- 独立扇区：未使用
```

#### 场景 C：已满一轮，从独立扇区恢复（对应场景 C 的回调）

```
初始状态（时间回调后）：
- 正式扇区 3：第 100-512 条有效（413 条）
- 正式扇区 0-2：全部有效（1536 条）
- 缓存扇区 0-2：已擦除
- 缓存扇区 3：99 条有效 + 401 条无效（但不使用）
- 独立扇区：99 条有效 + 新写入的记录（假设共 150 条）
- current_sector_type = TEMP
- temp_sector_cached_source = 3
- valid_count = 413 + 1536 + 150 = 2099 条
- formal_valid_count = 1949（正式扇区 3 的 413 条 + 正式扇区 0-2 的 1536 条）

操作：时间调快到 20:00（时间戳 25000）

步骤 1：扫描所有扇区
- 正式扇区 0-3：全部有效（2048 条，时间戳≤25000）
- 缓存扇区 3：500 条有效（时间戳≤25000）
- 独立扇区：150 条（但不再需要）

步骤 2：丢弃独立扇区
- 擦除独立扇区
- current_sector_type = CACHE
- current_sector_idx = 3（恢复使用缓存扇区 3）

步骤 3：更新元数据
- current_system_ts = 25000
- valid_count = 2048 + 500 = 2548 条
- formal_valid_count = 2048
- 保存元数据

步骤 4：继续写入
- 新记录写入缓存扇区 3 的第 501 条

结果：
- 有效记录：2548 条（恢复了 449 条！）
- 当前写入：缓存扇区 3 的第 501 条
- 独立扇区：已擦除
```

#### 场景 D：已满一轮，恢复已隐藏的缓存扇区（对应场景 D 的回调）

```
初始状态（时间回调后）：
- 正式扇区 2：第 50-512 条有效（463 条）
- 正式扇区 3：全部有效（512 条）
- 正式扇区 0-1：全部有效（1024 条）
- 缓存扇区 0-2：已擦除
- 缓存扇区 2：49 条有效（正在写入）
- 缓存扇区 3：100 条（逻辑上无效，但物理保留）
- current_sector_type = CACHE
- current_sector_idx = 2
- valid_count = 463 + 512 + 1024 + 49 = 2048 条
- formal_valid_count = 1999（正式扇区 2-3 的 975 条 + 正式扇区 0-1 的 1024 条）

操作：时间调快到 20:00（时间戳 25000）

步骤 1：扫描所有扇区
- 正式扇区 0-3：全部有效（2048 条）
- 缓存扇区 2：49 条有效
- 缓存扇区 3：100 条有效（时间戳≤25000，恢复有效！）

步骤 2：恢复缓存扇区 3
- 无需迁移数据（数据已在原位）
- current_sector_type = CACHE
- current_sector_idx = 3（切换回缓存扇区 3）

步骤 3：更新元数据
- current_system_ts = 25000
- valid_count = 2048 + 100 = 2148 条（恢复了 100 条！）
- formal_valid_count = 2048
- 保存元数据

步骤 4：继续写入
- 新记录写入缓存扇区 3 的第 101 条

结果：
- 有效记录：2148 条（恢复了 100 条！）
- 当前写入：缓存扇区 3 的第 101 条
- 独立扇区：未使用
```

---

## 三、读取策略

### 3.1 核心原则

```
1. 有效记录范围：
   - 总有效记录数 = valid_count
   - 正式扇区有效数 = formal_valid_count
   - 缓存扇区有效数 = valid_count - formal_valid_count

2. 读取第 K 条记录（逻辑索引，从 0 开始）：
   - 如果 K < formal_valid_count → 读正式扇区
   - 如果 K >= formal_valid_count → 读活动扇区（缓存扇区或临时扇区）

3. 正式扇区是环形存储：
   - 需要知道最旧的正式扇区索引（oldest_formal_sector）
   - 扇区索引 = (oldest_formal_sector + 扇区偏移) % 正式扇区总数

4. 缓存扇区/临时扇区：
   - 直接使用 current_sector_idx 或临时扇区
```

### 3.2 读取规则

```
读取第 K 条记录（逻辑索引，从 0 开始）：

步骤 1：判断 K 的范围
- 如果 K < formal_valid_count → 读正式扇区
- 否则 → 读活动扇区（缓存扇区或临时扇区）

步骤 2：计算物理位置

情况 A：读正式扇区（K < formal_valid_count）
  - sector_offset = K / SLOTS_PER_SECTOR
  - slot_index = K % SLOTS_PER_SECTOR
  - sector_index = (oldest_formal_sector + sector_offset) % FORMAL_SECTOR_COUNT
  - 读取正式扇区 sector_index 的第 slot_index 条

情况 B：读活动扇区（K >= formal_valid_count）
  - offset = K - formal_valid_count
  - 如果 current_sector_type == CACHE：
    - 读取缓存扇区 current_sector_idx 的第 offset 条
  - 如果 current_sector_type == TEMP：
    - 读取临时扇区的第 offset 条

步骤 3：读取数据并返回
```

### 3.3 读取示例

#### 示例 1：未满一轮，单个缓存扇区有效（场景 A 回调后）

```
状态：
- 正式扇区 0-1：已写满（1024 条，formal_valid_count = 1024）
- 正式扇区 2-3：空白
- 缓存扇区 0-1：已擦除
- 缓存扇区 2：80 条（正在写入）
- current_sector_type = CACHE
- current_sector_idx = 2
- valid_count = 1024 + 80 = 1104 条
- oldest_formal_sector = 0（最旧的是正式扇区 0）

读取第 500 条：
- 500 < 1024 → 读正式扇区
- sector_offset = 500 / 512 = 0
- slot_index = 500 % 512 = 500
- sector_index = (0 + 0) % 4 = 0
- 读取正式扇区 0 的第 500 条

读取第 1050 条：
- 1050 >= 1024 → 读缓存扇区
- offset = 1050 - 1024 = 26
- 读取缓存扇区 2 的第 26 条
```

#### 示例 2：未满一轮，时间回调后切换缓存扇区（场景 B 回调后）

```
状态：
- 正式扇区 0：512 条有效（formal_valid_count = 512）
- 正式扇区 1：逻辑上无效（时间戳>15000）
- 正式扇区 2-3：空白
- 缓存扇区 0-1：已擦除
- 缓存扇区 1：31 条有效（从正式扇区 1 复制过来，正在写入）
- 缓存扇区 2：100 条（逻辑上无效，时间戳>15000，不使用）
- current_sector_type = CACHE
- current_sector_idx = 1（当前写入位置）
- valid_count = 512 + 31 = 543 条
- oldest_formal_sector = 0

读取第 300 条：
- 300 < 512 → 读正式扇区
- sector_offset = 300 / 512 = 0
- slot_index = 300 % 512 = 300
- sector_index = (0 + 0) % 4 = 0
- 读取正式扇区 0 的第 300 条

读取第 530 条：
- 530 >= 512 → 读缓存扇区 1
- offset = 530 - 512 = 18
- 读取缓存扇区 1 的第 18 条

注意：
- 缓存扇区 2 的 100 条记录无效（时间戳>15000）
- 只读取当前正在写入的缓存扇区 1
```

#### 示例 3：已满一轮，使用独立扇区（场景 C 回调后）

```
状态：
- 正式扇区 3：第 100-512 条有效（413 条）
- 正式扇区 0-2：全部有效（1536 条）
- 缓存扇区 0-2：已擦除
- 缓存扇区 3：99 条有效（但不使用）
- 独立扇区：99 条 + 新写入的记录（假设共 150 条，正在写入）
- current_sector_type = TEMP
- temp_sector_cached_source = 3
- valid_count = 413 + 1536 + 150 = 2099 条
- formal_valid_count = 1949（正式扇区 3 的 413 条 + 正式扇区 0-2 的 1536 条）
- oldest_formal_sector = 3（环形缓冲的起始是正式扇区 3 的第 100 条）

读取第 500 条（逻辑索引）：
- 500 < 1949 → 读正式扇区
- 从正式扇区 3 的第 100 条开始算起
- 实际读取：正式扇区 3 的第 600 条（500+100=600，但 600>512，需要绕到下一个扇区）
- 更正：500 条对应正式扇区 3 的第 100+500=600 条逻辑位置
- sector_offset = 500 / 512 = 0（在第 0 个有效扇区偏移内）
- slot_index = 500 % 512 = 500
- 但正式扇区 3 只有第 100-512 条有效（413 条）
- 500 >= 413 → 需要绕到下一个扇区
- 实际：500 - 413 = 87 → 读正式扇区 0 的第 87 条

读取第 1000 条（逻辑索引）：
- 1000 < 1949 → 读正式扇区
- 1000 - 413（正式扇区 3） = 587
- 587 - 512（正式扇区 0） = 75 → 读正式扇区 1 的第 75 条

读取第 2000 条（逻辑索引）：
- 2000 >= 1949 → 读独立扇区
- offset = 2000 - 1949 = 51
- 读取独立扇区的第 51 条

注意：
- 读取顺序：正式扇区 3（第 100-512 条）→ 正式扇区 0 → 正式扇区 1 → 正式扇区 2 → 独立扇区
- 正式扇区 3 的前 99 条逻辑上无效（已复制到独立扇区）
- 只读取当前正在写入的独立扇区
```

#### 示例 4：已满一轮，时间回调后切换缓存扇区（场景 D 回调后）

```
状态：
- 正式扇区 2：第 50-512 条有效（463 条）
- 正式扇区 3：全部有效（512 条）
- 正式扇区 0-1：全部有效（1024 条）
- 缓存扇区 0-2：已擦除
- 缓存扇区 2：49 条有效（从正式扇区 2 复制过来，正在写入）
- 缓存扇区 3：100 条无效（时间戳>15000，不使用）
- current_sector_type = CACHE
- current_sector_idx = 2（当前写入位置）
- valid_count = 463 + 512 + 1024 + 49 = 2048 条
- formal_valid_count = 1999（正式扇区 2-3 的 975 条 + 正式扇区 0-1 的 1024 条）
- oldest_formal_sector = 2（环形缓冲的起始是正式扇区 2 的第 50 条）

读取第 500 条（逻辑索引）：
- 500 < 1999 → 读正式扇区
- 从正式扇区 2 的第 50 条开始算起
- 500 < 463（正式扇区 2 有效条数）→ 不成立
- 500 - 463 = 37 → 读正式扇区 3 的第 37 条

读取第 1000 条（逻辑索引）：
- 1000 < 1999 → 读正式扇区
- 1000 - 463（正式扇区 2） = 537
- 537 - 512（正式扇区 3） = 25 → 读正式扇区 0 的第 25 条

读取第 2000 条（逻辑索引）：
- 2000 >= 1999 → 读缓存扇区 2
- offset = 2000 - 1999 = 1
- 读取缓存扇区 2 的第 1 条

读取第 2047 条（逻辑索引，最后一条）：
- 2047 >= 1999 → 读缓存扇区 2
- offset = 2047 - 1999 = 48
- 读取缓存扇区 2 的第 48 条（最后一条）

注意：
- 读取顺序：正式扇区 2（第 50-512 条）→ 正式扇区 3 → 正式扇区 0 → 正式扇区 1 → 缓存扇区 2
- 正式扇区 2 的前 49 条逻辑上无效（已复制到缓存扇区 2）
- 缓存扇区 3 的 100 条记录无效（时间戳>15000）
- 只读取当前正在写入的缓存扇区 2
```

#### 示例 5：使用临时扇区（场景 A 或 C 回调后，正在使用独立扇区）

```
状态：
- 正式扇区 0-1：已写满（1024 条，formal_valid_count = 1024）
- 缓存扇区 0-1：已擦除
- 缓存扇区 2：19 条有效（但不使用）
- 独立扇区：50 条（正在写入）
- current_sector_type = TEMP
- temp_sector_cached_source = 2
- valid_count = 1024 + 50 = 1074 条

读取第 1050 条：
- 1050 >= 1024 → 读临时扇区
- offset = 1050 - 1024 = 26
- 读取临时扇区的第 26 条
```

---

## 四、元数据结构

### 4.1 元数据字段

```c
typedef struct {
    // 系统时间
    uint32_t current_system_ts;    // 当前系统时间的 Unix 时间戳
    
    // 有效记录计数
    uint16_t valid_count;          // 总有效记录数
    uint16_t formal_valid_count;   // 正式扇区有效记录数
    
    // 活动扇区写入位置
    uint16_t cache_write_next;     // 缓存扇区写入位置（下一条写入的索引）
    uint16_t temp_write_next;      // 临时扇区写入位置
    
    // 当前扇区状态
    uint8_t current_sector_type;   // 当前扇区类型：0=FORMAL, 1=CACHE, 2=TEMP
    uint8_t current_sector_idx;    // 当前扇区索引（0~N-1）
    
    // 临时扇区状态
    uint8_t temp_sector_active;    // 临时扇区是否激活：0=未使用，1=使用中
    uint8_t temp_sector_cached_source;  // 临时扇区缓存的源缓存扇区索引（0~N-1）
    
    // 保留字段
    uint8_t reserved;              // 保留（字节对齐）
    
    // 校验和
    uint32_t crc32;                // 元数据校验和
} DataManager_Meta_t;
```

### 4.2 扇区类型定义

```c
#define SECTOR_TYPE_FORMAL    0    // 正式扇区
#define SECTOR_TYPE_CACHE     1    // 缓存扇区
#define SECTOR_TYPE_TEMP      2    // 临时扇区
```

### 4.3 元数据保存时机

```
1. 修改系统时间后 → 立即保存
2. 活动扇区切换时 → 立即保存
3. 每写入 N 条记录后 → 定期保存（如每 10 条）
4. 系统空闲时 → 定期保存（防止掉电）
```

---

## 五、完整流程示例

### 5.1 场景：时间反复调整（未满一轮）

```
初始状态：
系统时间：2026-06-02 18:00:00（时间戳 20000）
正式扇区 0：已写满（512 条，时间戳 10000-15000）
正式扇区 1：空白（未使用）← 缓存扇区 1 没写满，正式扇区 1 是空白
正式扇区 2-3：空白（未使用）
缓存扇区 0：已擦除（数据已迁移到正式扇区 0）
缓存扇区 1：正在写入（第 100 条，时间戳 15001-20000）← 当前写入位置
缓存扇区 2-3：已擦除
总有效记录数：512 + 100 = 612 条
读取范围：从 正式扇区 0 的第 0 条 开始，到缓存扇区 1 的第 100 条

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 1 次调整：时间回调到 12:00（时间戳 15000）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1：扫描所有扇区
- 正式扇区 0：全部有效（512 条，时间戳≤15000）
- 正式扇区 1：空白（无数据）
- 正式扇区 2-3：空白
- 缓存扇区 1：100 条全部无效（时间戳 15001-20000 > 15000）

步骤 2：计算有效记录
- formal_valid_count = 512（正式扇区 0）
- valid_count = 512（缓存扇区 1 全部无效）

步骤 3：判断是否需要使用临时扇区
- 需要覆盖的是**正式扇区 1**（从第 0 条开始）
- 但正式扇区 1 是空白（因为缓存扇区 1 未写满，只有缓存扇区写满才会覆盖到正式扇区）
- **实际上需要覆盖的是缓存扇区 1**（从第 0 条开始）
- 因为覆盖的是**当前缓存扇区 1** → **使用临时扇区**
- **核心原则：只有临时扇区或缓存扇区写满的时候才会覆盖到正式扇区**

步骤 3a：处理临时扇区（不擦除缓存扇区 1！）
- 擦除临时扇区
- 缓存扇区 1 全部无效，无需复制
- temp_write_next = 0
- **缓存扇区 1 保持原样（100 条无效数据，但不擦除，时间调快时使用）**

步骤 3b：更新元数据
- current_system_ts = 15000
- valid_count = 512
- formal_valid_count = 512
- current_sector_type = TEMP（使用临时扇区）
- temp_write_next = 0
- temp_sector_cached_source = 1（缓存的是缓存扇区 1）
- 保存元数据

结果：
- 有效记录：512 条
- 当前写入：临时扇区的第 0 条
- 缓存扇区 1：**保持原样（100 条无效数据，但不擦除，时间调快时使用）**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 2 次调整：写入 30 条新记录（时间戳 15001-15300）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 写入临时扇区的第 0-29 条
- valid_count = 512 + 30 = 542
- temp_write_next = 30

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 3 次调整：时间调快到 20:00（时间戳 25000）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1：扫描所有扇区
- 正式扇区 0：全部有效（512 条，时间戳≤25000）
- 正式扇区 1：空白
- 正式扇区 2-3：空白
- 缓存扇区 1：100 条有效（时间戳 15001-20000 ≤ 25000，恢复有效！）
- 临时扇区：30 条有效（但不再需要）

步骤 2：丢弃临时扇区，恢复缓存扇区
- 丢弃临时扇区的 30 条
- 擦除临时扇区
- current_sector_type = CACHE
- current_sector_idx = 1（恢复使用缓存扇区 1）
- cache_write_next = 100（继续使用缓存扇区 1 的第 101 条）

步骤 3：更新元数据
- current_system_ts = 25000
- valid_count = 512 + 100 = 612
- formal_valid_count = 512
- 保存元数据

结果：
- 有效记录：612 条（恢复了缓存扇区 1 的 100 条！）
- 当前写入：缓存扇区 1 的第 101 条
- 临时扇区：已擦除

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 4 次调整：时间回调到 08:00（时间戳 8000）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1：扫描所有记录
- 正式扇区 0：部分有效（前 200 条，时间戳≤8000）
- 正式扇区 0 的后 312 条：无效（时间戳 8001-15000 > 8000）
- 正式扇区 1：空白
- 正式扇区 2-3：空白
- 缓存扇区 1：30 条全部无效（时间戳 15001-15300 > 8000）

步骤 2：计算有效记录
- formal_valid_count = 200（正式扇区 0 的前 200 条）
- valid_count = 200

步骤 3：判断是否需要使用临时扇区
- 需要覆盖的时间戳 8000 对应的物理位置：正式扇区 0 已写满，下一个应该是正式扇区 1
- **但正式扇区 1 是空白**（因为缓存扇区 1 未写满，只有缓存扇区写满才会覆盖到正式扇区）
- **实际上需要覆盖的是缓存扇区 1**（从第 0 条开始）
- 因为覆盖的是**当前缓存扇区 1** → **使用临时扇区**
- **核心原则：只有临时扇区或缓存扇区写满的时候才会覆盖到正式扇区**

步骤 3a：处理临时扇区（不擦除缓存扇区 1！）
- 擦除临时扇区
- 缓存扇区 1 全部无效，无需复制
- temp_write_next = 0
- **缓存扇区 1 保持原样（30 条无效数据，但不擦除，时间调快时使用）**

步骤 3b：更新元数据
- current_system_ts = 8000
- valid_count = 200
- formal_valid_count = 200
- current_sector_type = TEMP
- temp_write_next = 0
- temp_sector_cached_source = 1（缓存的是缓存扇区 1）
- 保存元数据

结果：
- 有效记录：200 条
- 当前写入：临时扇区的第 0 条
- 缓存扇区 1：**保持原样（30 条无效数据，但不擦除，时间调快时使用）**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 5 次调整：写入 20 条新记录
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 写入临时扇区的第 0-19 条
- valid_count = 200 + 20 = 220
- temp_write_next = 20

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第 6 次调整：时间调快到 22:00（时间戳 30000）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

步骤 1：扫描所有扇区
- 正式扇区 0：全部有效（512 条，时间戳≤30000）
- 正式扇区 1：空白
- 正式扇区 2-3：空白
- 缓存扇区 1：30 条有效（时间戳 15001-15300 ≤ 30000，恢复有效！）
- 临时扇区：20 条有效（但不再需要）

步骤 2：丢弃临时扇区，恢复缓存扇区
- 丢弃临时扇区的 20 条
- 擦除临时扇区
- current_sector_type = CACHE
- current_sector_idx = 1（恢复使用缓存扇区 1）
- cache_write_next = 30（继续使用缓存扇区 1 的第 31 条）

步骤 3：更新元数据
- current_system_ts = 30000
- valid_count = 512 + 30 = 542
- formal_valid_count = 512
- 保存元数据

结果：
- 有效记录：542 条（恢复了缓存扇区 1 的 30 条！）
- 当前写入：缓存扇区 1 的第 31 条
- 临时扇区：已擦除
```

---

## 六、关键要点总结

### 6.1 核心原则

1. **新记录始终写入缓存扇区**（或临时扇区）
2. **缓存扇区满后，迁移到正式扇区**
3. **正式扇区的数据保持稳定**，不轻易擦除
4. **时间调整时，只操作缓存扇区和临时扇区**
5. **读取时自动判断正式扇区 + 活动扇区**
6. **时间调快时，自动恢复之前隐藏的记录**

### 6.2 时间回调处理

```
核心原则：只有临时扇区或缓存扇区写满的时候才会覆盖到正式扇区

1. 扫描所有记录，确定有效范围
2. 计算有效记录数
3. 处理当前缓存扇区：
   - 有有效记录 → 保留并写回（或复制到临时扇区）
   - 全部无效 → **不擦除**（保持原样，时间调快时使用）
4. 判断是否需要使用临时扇区：
   - 如果需要覆盖的是当前缓存扇区 → 启用临时扇区，不擦除缓存扇区
   - 如果需要覆盖的是已完成的正式扇区 → 复制到对应缓存扇区，切换过去
   - 如果需要覆盖的是空白正式扇区 → 实际上是在缓存扇区上覆盖 → 启用临时扇区
5. 更新元数据
6. 继续写入新记录
```

### 6.3 时间调快处理

```
1. 扫描所有扇区
2. 恢复正式扇区的有效记录
3. 恢复缓存扇区的有效记录
4. 如果在临时扇区：
   - 丢弃临时扇区数据
   - 恢复使用原缓存扇区
   - 擦除临时扇区
5. 更新元数据
6. 继续写入
```

### 6.4 读取策略

```
1. 判断读取范围：
   - K < formal_valid_count → 读正式扇区
   - 否则 → 读活动扇区
2. 计算物理位置
3. 读取并返回
```

---

## 七、注意事项

### 7.1 Flash 擦除特性

```
1. Flash 擦除单位：扇区（4KB）
2. 擦除次数限制：约 10 万次
3. 写入特性：只能 0→1（不需要擦除）
4. 写入 1→0：必须先擦除整个扇区

优化策略：
- 减少不必要的擦除
- 优先使用 EMPTY 扇区
- 缓存扇区满后才擦除正式扇区
- 时间调整时不立即擦除
```

### 7.2 掉电保护

```
1. 元数据保存后立即写入 Flash
2. 关键操作前后保存元数据
3. 使用 CRC32 校验元数据完整性
4. 掉电后可从元数据恢复状态
```

### 7.3 磨损均衡

```
1. 记录每个扇区的擦除次数
2. 优先擦除次数少的扇区
3. 避免某些扇区过度磨损
```

---

## 八、待确认细节

（此处记录用户需要更正或补充的细节）

1. _待用户填写_
2. _待用户填写_
3. _待用户填写_

---

## 九、版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|---------|------|
| v2.0 | 2026-06-04 | 修复示例 3 中的环形计算错误（% 3 改为% 4） | AI Assistant |
| v2.1 | 2026-06-04 | 修正场景 B 的有效记录数（643→543），添加扫描所有扇区步骤 | AI Assistant |
| v2.2 | 2026-06-04 | 修正场景 C 和 D 的时间回调后有效记录计算，添加回调前写入位置说明 | AI Assistant |
| v2.3 | 2026-06-04 | 修正场景 A 和 B 的步骤 1 为"扫描所有扇区"，添加关键假设说明 | AI Assistant |
| v2.4 | 2026-06-04 | 修正完整流程示例（未满一轮场景），添加详细步骤和状态说明 | AI Assistant |
| v2.5 | 2026-06-04 | 修正参数定义（添加示例值说明），添加 temp_sector_cached_source 字段，修正时间回调处理流程 | AI Assistant |
| v2.6 | 2026-06-04 | 修正完整流程示例的缓存扇区索引（缓存扇区 3→缓存扇区 1，与正式扇区 1 对应） | AI Assistant |
| v2.7 | 2026-06-04 | 修正临时扇区使用条件：第 1 次回调不使用临时扇区（覆盖正式扇区），第 4 次回调使用临时扇区（覆盖当前缓存扇区），时间调快时丢弃临时扇区数据 | AI Assistant |
| v2.8 | 2026-06-04 | 修正临时扇区使用细节：使用临时扇区时不擦除缓存扇区（时间调快时恢复使用），时间调快时恢复缓存扇区的原写入位置 | AI Assistant |
| v2.9 | 2026-06-04 | 重写完整文档，修正第 1 次回调流程（不使用临时扇区，不擦除缓存扇区），第 4 次回调使用临时扇区（不擦除缓存扇区），第 6 次调快恢复缓存扇区 1 的 30 条数据 | AI Assistant |

---

**文档说明**：
- 本文档详细记录了 5 分钟历史记录覆盖方案的完整设计
- 包括 Flash 架构、时间调整流程、读取策略、元数据结构等
- 用户可在此文档上直接修改和补充细节
- 实现代码时应严格遵循本文档的设计
